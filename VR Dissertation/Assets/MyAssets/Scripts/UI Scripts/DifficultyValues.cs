public static class DifficultyValues
{
    // list of difficulties
    public enum Difficulties { easy, normal, hard };

    // set normal to the default selected difficulty
    public static Difficulties Difficulty = Difficulties.normal;
}